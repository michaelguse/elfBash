<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <table>
      <% for(var i=0; i < data.length; i++) { %>
         <tr>
           <td><%= data[i].Id %></td>
           <td><%= data[i].Name %></td>
         </tr>
      <% } %>
    </table>
  </body>
</html>
